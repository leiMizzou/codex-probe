# Candidate or explicit baseline provider credentials.
# Copy to .env locally if your shell tooling loads it, but do not commit .env.

PROVIDER_BASE_URL=https://candidate.example.com/v1
PROVIDER_API_KEY=your-api-key-here
